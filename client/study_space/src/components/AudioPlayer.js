import React from "react";

// creates an audio element with the given source
// the audio component always stays mounted
// only the playback-state changes
// the controls will only be visible if the control
// options is selected from the menu
function AudioPlayer({ audioSrc }) {
  return <div>hi i am an audio player</div>;
}

export default AudioPlayer;
